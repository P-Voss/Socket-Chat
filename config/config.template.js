
const config = {
    serverConfig: {
        ssl: false,
        host: "host",
        port: "port",
        ssl_key: "path/to/sslKey",
        ssl_cert: "path/to/sslCert"
    },
    mysqlConfig: {
        host: "host",
        user: "user",
        password: "password",
        database: "database"
    }
};

module.exports = config;
